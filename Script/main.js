// Setting Status for matches
setStatus(matches);

// Add functionality to schedule button
let scheduleButton = document.getElementById("schedule-button");
scheduleButton.addEventListener("click", openMatches);

// // setinterval to update the timer each 1 sec
// let myCountDownInterval = setInterval(function () {
//   countDown(12);
// }, 1000);

// Accessing Elements
// let matchesGroups = document.getElementsByClassName("match-group");
// matchesGroups[0].innerHTML = "Group A";
// matchesGroups[1].innerHTML = "Group B";
